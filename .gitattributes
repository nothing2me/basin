# This snapshot is verified byte-for-byte against data/manifest.json.
/data/observations.csv text eol=lf

# Windows batch files keep CRLF endings; cmd.exe can fail to find goto labels in LF-only files.
*.cmd text eol=crlf
